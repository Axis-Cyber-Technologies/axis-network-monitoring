{% extends "layouts/base.volt" %}

{% block title %}{{ lang._('Axis Network Monitor') }}{% endblock %}

{% block content %}
    <div class="content-box">
        <div class="content-box-main">
            <h2>{{ lang._('Axis Network Monitor') }}</h2>
            <p>{{ lang._('Hello World! Your plugin skeleton is ready.') }}</p>
            <p class="text-muted">
                {{ lang._('Use this page as a starting point for building LAN/WAN monitoring features.') }}
            </p>
        </div>
    </div>
{% endblock %}
